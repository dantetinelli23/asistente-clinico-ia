# openspec/config.yaml
# Auto-generated by sdd-init
schema: spec-driven

context: |
  Tech stack: Python 3.13, FastAPI, LangChain, LangGraph, ChromaDB, Cohere, Google GenAI, Groq
  Architecture: RAG-based clinical decision support — cyclic LangGraph agent with retrieval, ranking, generation, and human escalation. Exposed via FastAPI on Cloud Run.
  Testing: No test runner, no test files, no coverage, no linter/type checker/formatter detected. Testing infrastructure needs to be established from scratch.
  Style: Spanish domain language (clinical context), code identifiers are English. Vanilla Python modules under domain-oriented directories (agente/, retrieval/, ranking/, orquestacion/, observabilidad/).

strict_tdd: false

rules:
  proposal:
    - Include rollback plan for risky changes
    - State impact on existing LangGraph flow
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  runner:
    detected: false
    reason: "No pytest, unittest, or nose found in dependencies or config. No test files (test_*.py) exist in the project."
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
  quality:
    linter: false
    type_checker: false
    formatter: false
